#!/usr/bin/env bash
set -euo pipefail

# tools/edit — wrapper for BQN editor.

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
BQN_EDITOR="$DIR/edit-bqn"

exec "$BQN_EDITOR" "$@"
