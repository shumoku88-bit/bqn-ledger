#!/usr/bin/env bash
set -euo pipefail
root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$root"
exec bqn -e 'surface ← •Import "src/application/household_surface.bqn" ⋄ •Out ¯1↓surface.FormatTsv @'
