# Fixture: Budget Group Rename

This fixture verifies that the BQN calculations do not hardcode standard budget group names (`daily`, `flex`, `reserve`) and can handle custom names defined in `config.tsv` (e.g. `living_life`, `leisure`, `buffer`).
