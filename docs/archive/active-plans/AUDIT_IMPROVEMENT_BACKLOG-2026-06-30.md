# Audit improvement backlog 2026-06-30

Status: active backlog / docs-only
Scope: `bqn-ledger` program structure, BQN source, shell tools, checks, and documentation boundaries

This archived active-plan copy records the same audit improvement plan as `docs/AUDIT_IMPROVEMENT_BACKLOG.md`.
Keep the top-level document as the main reading entrance while this file marks the audit date.

Key tracks:

1. safety burrs around shell command execution
2. `tools/edit-bqn` dispatcher boundary
3. optional file loading correctness
4. centralized date logic
5. shell/BQN meaning boundary
6. report section contract consistency

See `docs/AUDIT_IMPROVEMENT_BACKLOG.md` for the full backlog, acceptance criteria, and PR slicing rule.
