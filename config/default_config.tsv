# default_config.tsv - Default global configuration fallback
# Format: key=value

# --- Canonical actual Journal ---
# This is the only actual source. Missing, duplicate, or empty values fail closed.
ACTUAL_JOURNAL_FILE=actual.journal

# --- Household envelope group policy labels ---
HOUSEHOLD_GROUP_LIFE=daily,flex
HOUSEHOLD_GROUP_RESERVE=reserve
HOUSEHOLD_GROUP_ORDER=daily,flex,reserve

# --- Special budget accounts (core internal logic) ---
BUDGET_PREFIX=budget:
BUDGET_ID_OPENING=budget:opening
BUDGET_ID_UNASSIGNED=none
BUDGET_ID_SPENT=budget:spent

# --- Policy profile choices ---
# Compatibility fallback only: new ledgers must explicitly choose envelope or none.
# Target contract: missing POLICY_BUDGET_STYLE will fail closed after a separate migration slice.
POLICY_BUDGET_STYLE=envelope
POLICY_RISK_STYLE=conservative
POLICY_INCOME_CADENCE=

# --- Optional execution envelope diagnostics ---
# If set, envelopes report compares this envelope remaining against unfinished planned payments.
EXECUTION_PLANNED_PAYMENTS_ENVELOPE=
