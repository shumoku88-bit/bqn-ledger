# Local environment variable overrides (template only).
# Copy this file to .env and adjust values. Never commit the actual .env.
LEDGER_DATA_DIR=/path/to/ledger-data/data

# Optional confirmation-gated MCP adapter. Use dummy values here only.
MCP_BEARER_TOKEN=replace-with-at-least-32-random-characters
MCP_HOST=127.0.0.1
MCP_PORT=3000
MCP_RUNTIME_DIR=/private/runtime/bqn-ledger-mcp
# MCP_ALLOWED_HOSTS=ledger.example.invalid
